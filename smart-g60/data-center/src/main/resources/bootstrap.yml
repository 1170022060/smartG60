# Tomcat
server:
  port: 9306
  tomcat:
    uri-encoding: UTF-8

# Spring
spring:
  main:
    allow-circular-references: true
  application:
    # 应用名称
    name: data-center
  profiles:
    # 环境配置
    active: online
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 192.168.2.200:8848
      config:
        # 配置中心地址
        server-addr: 192.168.2.200:8848
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}

# config
picturePath: "D:/gantry/picture"
pictureFailPath: "D:/gantry/pictureFail"